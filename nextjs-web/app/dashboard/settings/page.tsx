import Settings from "@/components/admin/Settings";

export const metadata = {
  title: "Settings – 3D Menu App",
  description: "Manage restaurant profile, branding, and notification preferences.",
};

export default function SettingsPage() {
  return <Settings />;
}
