ktor {
    deployment {
        port = ${?PORT} # Uses .env if available, defaults to 8080 in code
    }

    application {
        modules = [ com.example.backend.ApplicationKt.moduleWithEnv ]
    }
}

# ✅ Supabase config now lives in .env — this section is no longer needed
# supabase {
#     url = "https://bodlrtanetcvwfsmzqbw.supabase.co"
#     service_key = "YOUR_SUPABASE_SERVICE_KEY"
# }
